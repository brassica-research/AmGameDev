capture 29 — UI SIZING AND TRANSPARENCY: the paper becomes annotation rather than a lid. Narrower documents, translucent parchment so the field shows through, smaller type, fewer log lines, and the keybind list moved off the sheet onto a card that folds away after a few seconds (H pins it back).
